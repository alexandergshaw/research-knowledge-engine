/** @type {import('next').NextConfig} */
const nextConfig = {
  // TODO: Add environment variable validation
  // TODO: Configure image domains when Supabase Storage is integrated
  reactStrictMode: true,
};

module.exports = nextConfig;
