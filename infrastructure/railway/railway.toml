# railway.toml — Railway deployment configuration for apps/worker
# https://docs.railway.app/reference/config-as-code
#
# TODO: Populate when Phase 4 (worker deployment) begins.

[build]
builder = "NIXPACKS"

[deploy]
# TODO: Set the start command once the worker entrypoint is implemented
# startCommand = "python3 -m research_worker"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
